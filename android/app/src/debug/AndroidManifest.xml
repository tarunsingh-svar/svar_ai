<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds often point API_BASE_URL at a Flask server running over
         plain HTTP on the LAN. Release builds stay HTTPS-only, so this stays
         out of the main manifest. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
